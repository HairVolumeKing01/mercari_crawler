# Mercari JP 爬虫 使用说明

## 首次配置（只做一次）

### 1. 安装依赖
```bash
pip install selenium openpyxl pillow flask
```

### 2. 下载 ChromeDriver
去 https://googlechromelabs.github.io/chrome-for-testing/ 下载和你的 Chrome 版本匹配的 ChromeDriver。

解压后记住路径，默认路径在 `crawler.py` 里改（搜索 `_CHROMEDRIVER`）。

### 3. 代理
默认 `127.0.0.1:7897`。如果你代理端口不同，改 `crawler.py` 第 25 行，或设环境变量 `MERCARI_PROXY`。

---

## 搜索模式

```bash
# 基础搜索 + 存入数据库
python crawler.py search -k "关键词" --headless --db

# 搜多个关键词
python crawler.py search -k "关键词1" "关键词2" "关键词3" --headless --db
```

### 参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `-k` | 关键词，可多个空格分隔（必填） | — |
| `-n` | 爬取条数 | 50 |
| `--db` | 存入 SQLite 数据库 | `mercari.db` |
| `--db 文件名.db` | 存入指定数据库 | — |
| `--excel` | 同时导出 Excel | `mercari_items.xlsx` |
| `--excel 文件名.xlsx` | 导出到指定文件 | — |
| `--order` | 排序 | `created_time` |
| `--download 文件夹` | 下载图片到文件夹 | — |

### 排序方式

| 参数值 | 含义 |
|--------|------|
| `created_time` | 最新发布（默认） |
| `price:asc` | 价格低→高 |
| `price:desc` | 价格高→低 |
| `num_likes:desc` | 点赞最多 |

### 例子

```bash
# 搜一个词，爬 100 条，存数据库
python crawler.py search -k "ポケモンカード" --headless --db -n 100

# 搜三个词，各爬 50 条，合并到 pokemon.db
python crawler.py search -k "ポケモンカード" "ピカチュウ" "イーブイ" --headless --db pokemon.db -n 50

# 按价格低到高
python crawler.py search -k "ワンピース" --headless --db -n 30 --order price:asc

# 数据库 + Excel + 图片三件套
python crawler.py search -k "呪術廻戦" --headless --db --excel --download images
```

---

## 监控模式（自动盯新货）

```bash
# 基础监控（数据存数据库）
python crawler.py monitor -k "关键词" --headless --db --interval 15

# 数据库 + Excel 双保险
python crawler.py monitor -k "关键词" --headless --db --excel --interval 15

# 发现新品后推送到 QQ 邮箱，并每天发一封健康检查邮件
python crawler.py monitor -k "关键词" --headless --db --email --health-email --interval 15

# 多关键词轮询监控
python crawler.py monitor -k "关键词1" "关键词2" "关键词3" --headless --db --email --health-email --interval 15
```

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--interval` | 检查间隔（分钟） | 30 |
| `--db` | 存入 SQLite 数据库 | `mercari.db` |
| `--excel` | 同时导出 Excel | `mercari_monitor.xlsx` |
| `--email` | 发现新品后发送邮件提醒 | 关闭 |
| `--health-email` | 每天发送一封“监控仍在运行”邮件 | 关闭 |
| `--email-to` | 收件邮箱，多个用英文逗号分隔 | `SMTP_TO` 环境变量 |
| `--smtp-user` | SMTP 发件邮箱 | `SMTP_USER` 环境变量 |
| `--smtp-pass` | SMTP 授权码/密码 | `SMTP_PASS` 环境变量 |

每 N 分钟自动搜一次，发现新商品自动写入。`Ctrl+C` 停止。

### QQ 邮箱推送配置

QQ 邮箱需要先在邮箱设置中开启 SMTP 服务，并生成「授权码」。这里的 `SMTP_PASS` 填授权码，不是 QQ 登录密码。

推荐方式：复制 `config.example.json` 为 `config.json`，然后把里面的邮箱改成自己的。

```json
{
  "mercari_proxy": "http://127.0.0.1:7897",
  "monitor": {
    "keywords": [
      "カラオケ WIND BREAKER",
      "ちいかわ ハチワレ"
    ],
    "interval_minutes": 15
  },
  "smtp": {
    "host": "smtp.qq.com",
    "port": 465,
    "user": "你的QQ号@qq.com",
    "pass": "你的QQ邮箱授权码",
    "to": "接收提醒的邮箱@qq.com"
  }
}
```

之后直接运行：

```powershell
python crawler.py monitor -k "WIND BREAKER 桐生" --headless --db --email --health-email --interval 15
```

临时环境变量方式：

```powershell
$env:SMTP_USER="你的QQ号@qq.com"
$env:SMTP_PASS="你的QQ邮箱授权码"
$env:SMTP_TO="接收提醒的邮箱@qq.com"

python crawler.py monitor -k "WIND BREAKER 桐生" --headless --db --email --health-email --interval 15
```

如果发件人和收件人是同一个 QQ 邮箱，也可以让 `SMTP_USER` 和 `SMTP_TO` 填同一个地址。

邮件内容包含：商品图片、标题、价格、卖家、发布时间和 Mercari 原链接。

**支持多关键词同时监控**：另开终端跑不同关键词，都写同一个 DB。

```bash
# 终端1
python crawler.py monitor -k "WIND BREAKER 桐生" --headless --db --interval 15

# 终端2
python crawler.py monitor -k "呪術廻戦 缶バッジ" --headless --db --interval 15
```

---

## 网页仪表盘

```bash
python dashboard.py
```

> **⚠️ 仪表盘没有数据？** 爬虫命令必须带 `--db` 才会写入数据库，不带则只打印到终端。

浏览器打开 `http://127.0.0.1:5000`，可以看到：

- 🖼 商品卡片（缩略图、价格、发布时间、描述）
- 🔍 输入关键词实时筛选
- 🏷 按搜索关键词分标签查看
- 🔄 每 15 秒自动刷新
- 🗑 一键清空全部数据

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--port` | 端口号 | 5000 |
| `--db` | 数据库路径 | `mercari.db` |

**典型工作流**：
- 终端1：`monitor` 爬数据写库
- 终端2：`dashboard.py` 网页看数据
- 两者互不干扰，仪表盘自动刷新

---

## Excel 内容

| 列 | 内容 |
|----|------|
| ID | 商品编号 |
| 标题 | 商品名称 |
| 价格 | 日元价格 |
| 图片 | 内嵌缩略图 |
| 商品链接 | Mercari 链接 |
| 卖家 | 卖家昵称 |
| 点赞 | 点赞数 |
| 商品説明 | 卖家描述 |
| 评论 | 买家评论及卖家回复 |
| 发布时间 | 商品上架时间 |
| 爬取时间 | 数据抓取时间 |

同一文件多次运行自动追加，已有商品自动跳过。

删除 Excel 中不需要的行后，下次运行新数据会填入空白行。

---

## 数据存储结构

```
mercari.db（SQLite）
├── keyword      搜索关键词
├── name         商品名
├── price        价格
├── image_url    图片链接
├── url          商品链接
├── seller       卖家
├── likes        点赞数
├── description  商品説明
├── comments     评论
├── listed_at    发布时间
└── crawled_at   爬取时间

主键：(id, keyword)  → 同商品不同关键词分别保留
```

---

## 常见问题

**搜不到结果？**
- 确认代理已开，日本节点可用
- 浏览器能否打开 https://jp.mercari.com
- 用 `--headless` 参数（curl 模式拿不到 JS 渲染内容）

**数据不是最新的？**
- 确认 URL 中出现 `sort=created_time&order=desc`（已内置）
- Mercari 搜索索引本身有 5~15 分钟延迟

**发布时间显示「?」？**
- 爬虫命令必须加 `--db`，否则数据根本没写入数据库
- 如果加了 `--db` 仍然显示「?」，说明 Mercari 返回的时间格式解析失败（已修复，重新爬一次即可）
- 正常流程：第一步存基础数据，第二步 curl 补详情（含绝对时间）并写回

**SSL 报错？** 已内置忽略证书错误，理论上不会遇到。

**Chrome 崩溃？** 已内置 GPU 兼容参数和崩溃保护。如果频繁崩溃，检查内存是否充足。

**仪表盘连接不上？** 确认 `dashboard.py` 已另外启动，不是在 monitor 终端里访问。
